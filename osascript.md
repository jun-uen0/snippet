force shut down system
```shell
osascript -e 'tell app "System Events" to shut down'
```
restart system
```shell
osascript -e 'tell app "System Events" to restart'
```
open app
```shell
osascript -e 'tell app "Slack" to open'
```
control volume
```shell
osascript -e "set Volume 0
```